\documentclass[11pt,a4paper,openany]{mudbook}

% ============================================================================
% ADDITIONAL PACKAGES
% ============================================================================

% Use the epigraph package
\usepackage{epigraph}
\setlength{\epigraphwidth}{.6\textwidth}

% Use the Graphicx package to include PNG files
\usepackage{graphicx}

% Use the bytefield package
\usepackage{bytefield}

% Start making indices
\makeindex

% ============================================================================
% COMMANDS
% ============================================================================

\newcommand{\indexrm}[2][]{\textrm{#2}\index{#2#1}}
\newcommand{\indextt}[2][]{\texttt{#2}\index{#2@\texttt{#2}#1}}

\begin{document}

% Set the C++ listing style
\definecolor{darkgreen}{RGB}{0, 95, 0}
\definecolor{lightgrey}{RGB}{242,242,242}
\lstset{%
  language=C++,
  basicstyle=\scriptsize,
  commentstyle=\color{darkgreen},
  backgroundcolor=\color{lightgrey},
  framesep=5pt,
  framexbottommargin=2pt,
  showstringspaces=false}

% ============================================================================
% TITLEPAGE
% ============================================================================

\author{Stefan Sinnige\\ Kalion}
\date{April 2020}
\begin{titlepage}
  \vspace{20mm}
  \begin{center}
    \includegraphics[width=50mm]{mud.png}
  \end{center}
  \vspace{10mm}
  \begin{center}
    {\LARGE \MUDLib Manual}
    \vskip 3em
    {\large Stefan Sinnige\\ Kalion}
  \end{center}
\end{titlepage}

% ============================================================================
% COPYRIGHT
% ============================================================================

\newpage
\thispagestyle{empty}
~\vfill

\noindent This book is typeset in \fontfamilyname, which is licensed under the
\fontfamilylicense.
\vspace{1em}

\noindent Copyright \copyright\ 2020, Kalion.
\vspace{1em}

\noindent PLACEHOLDER FOR COPYRIGHT NOTICE.
\vspace{1em}

\noindent \textit{First release, April 2020}

\noindent \textit{This release, April 2020}

% ============================================================================
% TABLE OF CONTENTS
% ============================================================================

\pagestyle{empty}
\tableofcontents
\pagestyle{fancy}

% ============================================================================
% PREFACE
% ============================================================================

\chapter*{Preface}
\addcontentsline{toc}{chapter}{Preface}

\epigraph{\textit{Omne tulit punctum qui miscuit utile dulci,\\
lectorem delectando pariterque monendo.}\\
He who blends usefulness and pleasantness wins every
vote, at once delighting and instructing the reader.}%
{Horace - Ars Poetica (343-344)}\index{Horace}

When Horace postulates in his "Ars Poetica" (19 B.C.) that one of the
qualifications of a poet is to mix the usefulness with sweetness in order to
both delight and instruct the reader, little would he have known that this
would not only apply to the written art of poetry, but to the art of
programming as well. Taken from verse 343 of this poem is the Latin phrase
\textit{miscere utile dulci} that captures this principle. Abbreviated to \MUD,
is the project name that is being uses as the basis for naming various
projects. Included in this, is the \MUD Library.

\MUDLib is a general purpose application framework library that conceptualises
various aspects of \Cpp application development. A number of such frameworks
already exists and have been quite successful. So why is there a need for yet
another one? Well, there probably is not really a need for it, but I will
refer to the premise in the previous paragraph. It's intention is not only to
be useful and delightful for anyone who may find it suitable for their own
purposes, but equally so, it will be useful and delightful for the author
who created it. This includes the ever evolving \Cpp language constructs as
well as the need to abstract and combine many principles of application
development into a consistent single framework.

Let's take, as an example, the technology behind programming a TCP server and
client application. Back in 1983, the Berkeley Sockets API (application
programming interface) was introduced to define the functions to support
creating of sockets, establishing and accepting connections and sending and
receiving data from one endpoint to another. This API was adopted into the
POSIX (Portable Operating System Interface) standard and is supported on
almost any platform. For example, it is incorporated into UNIX systems and
their derivations, like Oracle Solaris, Linux and Apple Darwin, and also in
Microsoft Windows. Even to this day, applications written in the 80s would
still be able to build and run on modern operating systems.

Throughout the decades, advances has been made in all aspects of computer
science. The adoption of object-oriented programming is one of those advances
and led to the introduction of \Cpp and other programming languages. This has
created some a dilemma that the procedural API for handling sockets of POSIX
has to be mapped within an object-oriented framework. Some library providers
have adopted these concepts successfully and these have been widely adopted,
even to the point that they have been incorporated into \Cpp standards (like
some aspects of the Boost library).

However, with all these different libraries, there is no single abstraction of
such concepts which is a 'correct' one. An ACE (Adaptive Communication
Environment) socket differs in a Boost socket by their viewpoint on how a
socket can be represented. Although there is inevitably a great number of
overlap of functionality that each has to provide, the differences between them
and the design concepts they implemented, needs to be assessed to their
usefulness in the specific problem domain and which can be applied more
effectively.

Another aspect of application framework libraries is that there are many
libraries that focus on a certain key feature, but there are only a few that
cater an entire spectrum. There are a predominantly a few popular libraries
for handling SSL, XML or SOAP communication, and most of them are integrated
in a certain way in an application framework. However, that makes a consistent
implementation of such a framework harder. Of course, one would gain an awful
lot from the experience of such mature libraries, but they may not be a
prefect fit for the concept that the application framework would like
to convey. Such libraries are often integrated with an adapter pattern to make
them fit within the confinements of the application framework, even though some
concessions may need to be made in the process.

In this aspect, \MUDLib serves the purpose foremost, to present yet another
view of the current technologies and how they can be applied in application
development. It is advocating a certain viewpoint on how different
technologies can operate together, with the same underlying concepts and
methodologies, while providing a consistent application framework on all
levels.  Therefore, it is aiming to implement many of the technologies on a
\textit{per need} basis. It is not trying to be complete in every aspect it
supports, but only implements those aspects that are vital for a certain
application. This library is therefore ever evolving!

% ============================================================================
% GUID FOR READERS
% ============================================================================

\chapter*{Guide For Readers}
\addcontentsline{toc}{chapter}{Guide For Readers}

% ============================================================================
% PART
% ============================================================================

\part{Introduction}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Tour of \MUDLib: Methodologies}

The methodologies that form the basis of this library. Talk about namespaces, streaming, C++11,
perhaps even formatting?

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Tour of \MUDLib: Components}

Talk about the component layering, namespace inclusion. Which components, the functions they
provide and the support they give to other layer. Be clear that dependency of layers is only one-way!

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Tour of \MUDLib: Unit testing}

% ============================================================================
% PART
% ============================================================================

\part{Core}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Exceptions}
\label{chp:core-exceptions}

Exceptions are used in the \MUDLib library for signalling an erroneous condition. Although exceptional behaviour does not necessarily mean conditions
that rarely occur, they can be a useful tool in situations where certain
error conditions are excepted. For example, an application may choose to raise
an exception when a connection cannot be established to a remote machine.
This in itself is not a rare occasion, but can actually be expected at times.
It just depends on what the application does when such an exception is caught.
The most crude way of handling exceptions is to terminate the application,
but that is a very disastrous method of dealing with such a scenario. Rather,
by carefully planning and handling exceptions, it may trigger an attempt to
reconnect instead. 

Apart from the standard exception that are provided with the \Cpp standard,
additional ones have been defined that are used within the library. 

\section{Exception Classes}
\label{sct:core-exception-classes}

\subsection{\texttt{not\_owner}}

The \indextt{not\_owner} exception is used to indicate that an operation is
attempted on a resource which is currently not the \textit{exclusive} owner
of that resource. Exclusive ownership is often represented through a
\texttt{unique\_ptr}, where a \texttt{nullptr} value of the underlying
pointer indicates that the user is currently not the owner. This exception can
be thus to be thrown in situations like in listing~\ref{lst:not_owner}.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={The \texttt{not\_owner} exception.},
  label=lst:not_owner]
if {ptr == nullptr} {
    throw mud::core::not_owner();
}
\end{lstlisting}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Factory}
\label{chp:core-factory}

A factory is a common pattern to create instances of an class based on a
specific key. All the concrete classes registred to the factory are derived
from a common abstract class.

The key can be an enumeration type or a \texttt{std::type\_index}. The
enumeration can create an associated concrete class (i.e. the concrete class
is not known, but the identifying key is). While the \texttt{std::index\_type}
creates the object directly by the type of the class.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={TBD}
  label=lst:TBD]
// Factory by enumeration key
typedef mud::core::factory<Base::type_t, Base> BaseFactoryByKey;
BaseFactoryByKey::registrar<Base::type_t::DERIVED, Derived> registrar_derived;

auto object = BaseFactoryByKey::instance().create(Base::type_t::DERIVED);

// Factory by class type
typedef mud::core::factory<std::index_type, Base> BaseFactoryByType;
BaseFactoryByKey::registrar<Derived> registrar_derived;

auto object = BaseFactoryByKey::instance().create(typeid(DERIVED));
\end{lstlisting}

Factories can use creator function with or without any arguments.

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Handles}
\label{chp:handles}

The core library defines an abstract concept of a \indexrm{handle} as a
unique reference to s single resource of any kind. This can be resources that
have a direct link with an operating system supported device but can also refer
to a reference to a software construct, like a window \indexrm[!window]{handle}.

In either case, each of these handles are represented as an instantiation of a
\indextt{basic\_handle}. This class is templated with the underlying type of
he resource. For example, POSIX style kernel handles are represented by an
nteger type, while Microsoft Windows window handles are of type \texttt{HANDLE}.
The \texttt{basic\_handle} simply defines the constraints that are put on these
handle types, and are:
\begin{itemize}
\item No default constructor
\item Not copy-constructable
\item Not copy-assignable
\item Only move-constructible
\end{itemize}
Furthermore, the handle can return its underlying representation using the
type-operator.

When a handle is not valid (i.e. not referring to any resource), it shall be
deemed as being invalid. The underlying resource will then refer to an
invalid representation, for exmaple, to a \texttt{nullptr} for pointer typed
resources.

In general, the underlying resource is abstracted away behind the 
\texttt{handle} class and in most cases does not need to be accessed. However,
in cases where it is required to access the underlying resource value, it
should then be known what that underlying type should be and can then be
accessed using the \indextt{internal\_handle} template as presented in
listing~\ref{lst:internal_handle}.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Accessing underlying resource handle values.},
  label=lst:internal_handle]
std::unique_ptr<mud::code::handle> handle_ref = ...;
int h = internal_handle<int>(handle_ref);
\end{lstlisting}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Endian}
\label{chp:endian}\index{endian}

A concept that often raises its head when dealing with platform independent
structures and tecnologies is the notion of endianness. It defines a system's
representation of multi-byte integral values in memory. In \textit{big-endian}
\index{big endian} systems, the most significate byte is stored at the
smallest memory address, while in \textit{little-endian}\index{little endian}
systems, the most sugnification byte is stored at the largest memory address.

Platform independent networking techologies often opt for a big endian value
representation (also known as \textit{network order}\index{network order}). In
systems whose architecture is based on little-endian, every representation of
a mutli-byte integral value needs to convert its value when reading from
a network protocol or writing to a network protocol. The \indextt{endian}
class can assist in that conversion.

The \texttt{endian} class can assess the current architecture to determine
its native endian system. It can also convert any native integral to any
endian system, and vice versa. The example in listing~\ref{lst:endian}, show
how to represent a native 32-bit integer to a big-endian system.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Converting between endian systems.},
  label=lst:endian]
uint32_t value = 0x12345678;
uint32_t big = mud::core::endian::convert(
        mud::core::endian::native(),
        mud::core::endian::endian_t::big,
        value);
\end{lstlisting}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Tasks}
\label{chp:tasks}\index{tasks}

A task is a finite operation that can be performed on its own, and which can
potentially be performed in an asynchroneous way. The  \MUD
\indextt{task} is conceptually identical to a \texttt{std::packaged\_task}. The
creator of the task can request a \indextt{std::future} and be notified when
the task has finished its execution.

As a task is an instance of a specified execution, it can be moved around
as long as it has not started execution. This allows a task to be created,
stored in a container or being passed on to a separate thread.

In its most rudimentary example, a task can be created and executed as in
listing~\ref{lst:task-lambda}. This example created a task object, but when
executed, it requires additional parameters to be passed in. The result can
either be available as shown, or be returned as part of a \texttt{std::future}.
When using \texttt{std::bind}, a task can be bound to input parameters at
creation time.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Lambda task with parameters and return value.},
  label=lst:task-lambda]
mud::core::task<int(int, int)> mult([](int i, int j) {
    return i*j;
  });
int result = mult(2, 3);
\end{lstlisting}

Any exception that is thrown while executing the task is captured within
the \texttt{std::future}.

A task can also be executed on a different thread, for example by using a
\texttt{std::thread} or by using the \indextt{task\_worker}. The worker is a
\texttt{std::thread} that is associated with a \indextt{task\_queue}. The queue
is a queue container onto which tasks can be pushed and tasks can be popped.
This is implemented in a thread-safe manner such that threads can push and pop
freely.

As the task worker is a single thread that can executes tasks from a queue,
a \indextt{task\_worker\_pool} is a pool of workers that can all pop and
execute tasks independently of the same queue.  Listing~\ref{lst:task-pool}
shows an example of a task pool that can execute simple tasks. A
\indextt{simple\_task} is a task that has no arguments when executed and does
not return a return value. Similarly, a \indextt{simple\_task\_queue} is a
queue for \texttt{simple\_task}'s; a \indextt{simple\_task\_worker} is a worker
executing a \texttt{simple\_task}; and a \indextt{simple\_task\_worker\_pool}
is a pool of \texttt{simple\_task\_worker}.
\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Task pool executing simple tasks.}
  label=lst:task-pool]
mud::core::simple_task_queue queue = std::make_shared_ptr<mud::core::simple_task_queue>();
mud::core::simple_task_worker_pool pool;

// Any thread can push tasks, for example
mud::core::simple_task tsk([](){
    // Execute something on a separate simple_task_worker thread
  });
queue.push(std::move(tsk));

// Stop the workers in the pool
pool.stop();
\end{lstlisting}

% ============================================================================
% PART
% ============================================================================

\part{Events}

Building interactive applications, or applications that react on various
input stimuli, are often designed around an event framework. This allows the
applciation to define event types and what to do when a certain event
eventuates. This can include events like pressing a button on a keybaord,
dragging the mouse cursor, an entry is added to a database or an HTTP request
has been received. Many of these event frameworks are pre-existing and already
provided by the operating system or some other software development kit.

These pre-existing frameworks, however, cater for only a very specific subset
of events. An event framework that listens to UI input uses a different kind
of event technology than listing for an incoming TCP packet. This makes
applications that integrate with multiple event frameworks at once, quite
an 'eventful' exercise.

The event framework provided by the \MUDLib library aims to overcome this by
creating a single event frameowkr interface that creates a facade to the
underlying pre-existing technologies.

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Event abstraction}

At the heart of the event framework is the concept of an \indextt{event} and
an event-loop that runs forever, listening for any kind
of event and invoking a user-defined handler when such an event occurs.

The event itself is a container that holds three key properties:

\begin{itemize}
  \item The \indextt{handle} to the resource that is associated to that event.
        This can be a file descriptor\index{file descriptor},
        a window handle\index{window handle}, etc.
  \item A signal mask to define what to listen for on that resource
  \item A handler to call when an event occurs
\end{itemize}

\noindent All these properties are maintained within the \texttt{handle} base
class.

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Event Loops}
\label{cpt:event-loops}

In modern software architecture, event-loops play a significant role. It allows
the application to be reactive on eny event while in the mean-time continue
with other tasks. It also brings a level of abstraction to the application
as a single event-loop can be used for a variety of events. For example, the
POSIX \indextt{select} mechanism allows an application to wait for multiple
file-descriptors with one call. When any of these file-descriptors
detect an event, the application can examine the \texttt{select} structure to
determine which file descriptor has been triggered.

Over time, a number of event-loops have been developed and are widely in use,
each to serve their own purpose or software architecture, and to extend the
same principles in other realms. As \texttt{select} has been widely used to
handle file-descriptors, it is not designed to be used to handle graphical
user interface events (like moving a window or selecting a check-box).

This leads to applications having to implement multiple event-loops, each
with their own architecture and design criteria, at the same time where each
event-loop handles their own specific event.

Taking a holistic approach, an event-loop is nothing more than a mechanism to
register the interest of an event and be signalled when such an event occurs.
It does not matter if that event is a network socket that receives a new
message, or a check-box that has been selected. By hiding the implementation
of these architecture dependent event-loops, it is possible to create a
platform independent event-loop. It also allows abstraction of different
implementation of common technologies. For example, a \textit{pipe}\index{pipe}
can be used for inter- and intra-process communication, but they are defined
differently on UNIX based systems where the pipe is a file-descriptor that uses
the \texttt{select} mechanism, and on Microsoft Windows system that uses a
\indextt{HANDLE} and a \indextt{WaitForMultipleObjects} mechanism to wait for
any data arriving on the pipe. Since the concept of a pipe is already abstracted
behind a \indextt{handle} abstraction, the event-loop can register that
handle using the appropriate mechanism as well. On UNIX system, the pipe handle
is an integer file-descriptor that registers the pipe with  the
\texttt{select} mechanism, while on Microsoft Windows systems, the pipe handle
is a Windows \texttt{HANDLE} and regsiters the pipe with the
\texttt{WaitForMultipleObjects} mechanism.

\section{\texttt{event\_loop}}
\label{sct:event_loop}

Most operating systems have means available to let the kernel wait for any
event on a kernel handle and inform an application that was actively waiting
for that to occur. That concept is the driving factor behind the
\indextt{kernel\_event\_loop}. Kernel handles can be registered with the
event loop together with an associated function that is to be invoked when
an event is triggered on the handle.

When the application is running the event loop, using the \texttt{run} method,
then the thread that issued this method will not return until the event loop
is requested to terminate. This request can be issued by any other thread,
and even by one of its event handlers. It is therefore quite common that the
event loop would run on its own thread or as an asynchronous operation, as in
illustrated in listing \ref{lst:event-loop-thread}.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Kernel event loop thread},
  label=lst:event-loop-thread]
// Start the event loop
mud::event::event_loop loop;
std::future<void> future = std::async(
    std::launch::async, [&loop]{} {
        loop.loop();
    });

// Register a handler and a 'read' handler
loop.register_handler(mud::event::event(a_handle, []() {
        // Handle the event, e.g. read from a socket.
    }), mud::event::event::signal_t::READING);
    
// Do some action. Any write to a_handle would cause the
// event handler to be called, indicating to read.

// Terminate the event loop.
loop.terminate();
\end{lstlisting}

A singleton global event-loop is readily available and can be used as the
main event-loop of any application (\texttt{kernel\_event\_loop::global()}).
If needed, other event-loops can be used simultaneously, each handling their
own specific set of events.

% ============================================================================
% PART
% ============================================================================

\part{I/O}

The concept of I/O is a fundamental one in application programming. The
majority of applications would have some concept of using I/O for any kind
of communication. This includes communication with hardware, other software
applications or services and interaction with humans. Input can come from
resources like files, keyboard or through an internet socket connection; while
output can be a display, a printer, a file or perhaps a row in a database.

As can be seen, there is a whole variety of I/O and at various levels. The
types of I/O that are dealt with in this layer of the \MUDLib library is the
low-level operating system supported I/O. This is typically I/O that is
supported by, for example, the POSIX standard and would typically require a
method where multiple kernel events can be examined and processed (e.g.
\indextt{select}, \indextt{poll} and \indextt{kevent}).

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Stream Buffers}
\label{chp:stream-buffers}

The \indextt{stream\_buffer} class is a specialised \indextt{std::streambuf}
that is capable of reading from and writing to a \indextt{kernel\_handle}. The
underlying implementation that performs the direct access to the resource
is the POSIX \indextt{read} and \indextt{write} routines. The data when reading
from the device is buffered in an internal buffer. When the buffer is exhausted
and more data is required to be read from the device, the \texttt{underflow}
method is called. If that device is set-up in a \textit{blocking} mode and
there is not enough data, then the call may be blocked until the data is
available, or until the device is no longer accessible.

The same holds for writing to the device. As the data is buffered, the actual
writing to the device may be delayed, for example, when the buffer becomes
full. The writing can be enforced by \textit{flushing} the stream buffer.

The stream buffer is often not used by itself, but through an output
stream instead.

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Pipes}
\label{chp:pipes}

A \indextt{pipe} defines the concept of a uni-directional data channel.
Both ends of the pipe have their own unique \indextt{kernel\_handle}. One end,
the source, is the side that is attached to the output stream and is used to
end data over the pipe to the other side. That side, the sink, is the side that
is attached to the input stream and is used to receive the data from the other
side.

\begin{figure}[hbt]
  \centering
  \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
      \umlclass[x=0,y=11]%
          {mud::io::pipe}%
          {}%
          {+ pipe() \\
           + {\textit \~{}pipe} \\
           + istr() \\
           + ostr() \\
           + read\_handle() \\
           + write\_handle() \\
          };
      \umlclass[x=-5,y=9.8]%
          {std::ostream}%
          {}%
          {
          };
      \umlclass[x=5,y=9.8]%
          {std::istream}%
          {}%
          {
          };
      \umlclass[x=-5,y=3,name=ostream-streambuf]%
          {mud::io::streambuf}%
          {}%
          {+ overflow(int): int \\
          };
      \umlclass[x=5,y=3,name=istream-streambuf]%
          {mud::io::streambuf}%
          {}%
          {+ underflow() : int \\
          };
       \umlclass[x=-2,y=6,name=ostream-handle]%
          {mud::io::kernel\_handle}%
          {}%
          {
          };
       \umlclass[x=2,y=6,name=istream-handle]%
          {mud::io::kernel\_handle}%
          {}%
          {
          };
       \draw[aggregation] (stdostream.east) -- (mudiopipe.west);
       \draw[aggregation] (stdistream.west) -- (mudiopipe.east);
       \draw[aggregation] (ostream-handle.north) -- +(0,1) -|
                          (mudiopipe.south);
       \draw[aggregation] (istream-handle.north) -- +(0,1) -|
                          (mudiopipe.south);
       \draw[dependency]  (stdostream) -- (ostream-streambuf);
       \draw[dependency]  (stdistream) -- (istream-streambuf);
       \draw[dependency]  (ostream-streambuf.east) -| (ostream-handle);
       \draw[dependency]  (istream-streambuf.west) -| (istream-handle);
    \end{tikzpicture};
  }
  \caption{\texttt{mud::io::pipe} class diagram.}
  \label{fig:mud-io-pipe-class}
\end{figure}

Figure~\ref{fig:mud-io-pipe-class} illustrates the relationship of all the
classes involved in the \texttt{pipe}. The pipe itself keeps an instance of
the \indextt{std::streambuf} instances that handle the buffered input and
output to the source and sink end of the pipe. In order to stream data over
the pipe, a reference to the input and output stream is acquired from the pipe
which is automatically attached to the appropriate stream buffer.

\section{Writing to the pipe}

Writing to a pipe can use all the standard functionality that is provided
by the \indextt{std::ostream}. This includes formatted stream functionality
and unformatted (binary) output.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Writing to the pipe},
  label=lst:writing-pipe]
// Create a pipe
mud::io::pipe p;

// Formatted output.
p.ostr() << "Hello" :: std::flush();

// Unformatted output (binary).
uint8_t block[] = { 0x01, 0x00, 0x81, 0xF4 };
p.ostr().write((const char*) block), 4) << std::flush();
\end{lstlisting}

\section{Reading from the pipe}

Reading from a pipe can use all the standard functionality that is provided
by the \indextt{std::istream}. This includes formatted stream functionality and
unformatted (binary) input.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Reading from the pipe},
  label=lst:reading-pipe]
// Create a pipe
mud::io::pipe p;

// Formatted input. This may block until data is available.
std::string str;
p.ostr() >> str;

// Unformatted input (binary).
uint8_t block[4];
memset(block, 0, 4);
p.istr().read((char*)block, 4);
\end{lstlisting}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Network}
\label{chp:network}

Network programming establishes connection between two network endpoints using
the Open Systems Interconnection (OSI)\index{OSI} model. The OSI model defines
seven layers \cite{ISO7489}, ranging from the physical layer (e.g. ethernet,
wireless, bluetooth, CAN bus) to the application layer (e.g. HTTP, LDAP, SSH).
From a software point of view, up to the transport layer are provided by the
operating system kernel and anything above that needs to be represented by
user-space libraries. Each layer provides the building blocks for the layer
above.

\begin{figure}[hbt]
  \centering
  \begin{bytefield}[bitwidth=60mm]{1}
    \bitbox{1}{7: Application Layer}\\
    \bitbox{1}{6: Presentation Layer}\\
    \bitbox{1}{5: Session Layer}\\
    \bitbox{1}{4: Transport Layer}\\
    \bitbox{1}{3: Network Layer}\\
    \bitbox{1}{2: Data Link Layer}\\
    \bitbox{1}{1: Physical Layer}
  \end{bytefield}
  \caption{OSI Network Layers.}
  \label{fig:osi}
\end{figure}

\MUDLib provides functionality to support some of the core networking protocols
in these layers and focuses primarily on the protocols that are used in
the Internet Protocol Suite. This includes the widely used Transmission Control
Protocol\index{Transmission Control Protocol} (TCP)\index{UDP} and User Datagram
Protocol\index{User Datagram Protocol} (UDP)\index{UDP} protocols defined in
the Transport Layer.

\section{Network Layer}

The Network Layer (layer 3 in the OSI model) is responsible for providing
the means to connect transport entities (connection and connection-less) and
to exchange data. The protocol that is widely used in the Internet Protocol
Suite in this layer is the IP protocol, but notable others include ICMP
and IPsec.

\subsection{IP}

The network layer defines (amongst others), the Internet Protocol
\index{Internet Protocol} (IP)\index{IP}. This if the principal protocol in
the internet protocol suite and is used to relay data packets across the
network boundaries. \MUDLib has only support for the Internet Protocol
version 4 (IPv4), even-though version 6 (IPv6) is becoming more widely spread.
Later editions may include support for version 6.

\begin{figure}[hbt]
  \centering
  \begin{bytefield}[bitwidth=0.9em]{32}
    \bitheader{0,15,16,31}\\
    \bitbox{4}{Version} & \bitbox{4}{IHL} & \bitbox{8}{TOS} & \bitbox{16}{Total Length} \\
    \bitbox{16}{Identification} & \bitbox{3}{Flags} & \bitbox{13}{Fragment offset}\\
    \bitbox{8}{TTL} & \bitbox{8}{Protocol} & \bitbox{16}{Header Checksum}\\
    \wordbox{1}{Source Address}\\
    \wordbox{1}{Destination Address}\\
    \wordbox[lrt]{1}{Options}\\
    \wordbox[lrb]{1}{...}\\
    \wordbox[lrt]{1}{\textit{Data}}\\
    \skippedwords\\
    \wordbox[lrb]{1}{}
  \end{bytefield}
  \caption{IP Protocol Packet.}
  \label{fig:ip-packet}
\end{figure}

\subsubsection{Addresses}

In order to transmit packets from one system to another, these systems
needs to be identified such that the packets can be routed successfully.
IPv4 uses a 32-bit address space, conveniently divided into four 8-bit
numbers. This allows for a human representation using \textit{dotted decimals}
\index{dotted-decimal notation} like \texttt{192.168.2.23}, which is
represented by a 32-bit number of \texttt{0xC0A80217}.

The \indextt{ip::address} class is an abstraction of the IPv4 address
representation. Addresses can be specified by using a string representing a
dotted-decimal, or directly as a 32-bit value (in network byte order),
represented by the POSIX \indextt{in\_addr\_t} structure.

Once the socket has been bound to a local address or when a connection to a
remote peer~\index{peer}\index{socket!peer} has been established, the address
of the local and remote peer is available through the \indextt{source\_address}%
\index{socket!source address} and the \indextt{destination\_address}%
\index{socket!destination address} methods.

\subsubsection{Sockets}

The Network Layer socket consist of the \texttt{basic\_socket}
\texttt{basic\_socket} that holds the reference to the socket resource handle
and the \indextt{ip::socket}. These do not provide the direct functionality of
sending and receiving data, but rather the concepts of representing the Network
Layer socket model. The \texttt{basic\_socket} defines the framework for a
socket that can be used on \textit{all} higher layered communication protocols.
It does provide the definition of the socket's communication domain (i.e.
IPv4\index{IPv4} and IPv6\index{IPv6}), the type of the socket (i.e. stream,
datagram or raw) and the communication protocol (i.e. TCP\index{TCP},
UDP\index{UDP} and ICMP\index{ICMP}). This combination is often constrained by
the capabilities provided by the underlying platform's operating system.

\section{Transport Layer} 

The Transport Layer (layer 4 in the OSI model) is responsible for providing
peer-to-peer communications to applications. These communications can be
connection-oriented like \index{Transmission Control Protocol} (TCP)\index{TCP}
and connection-less like \index{User Datagram Protocol} (UDP)\index{UDP}. These
are the two main protocols at this layer and the only ones that are directly
supported.

\begin{figure}[hbt]
  \centering
  \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
      \umlclass[x=4,y=6,name=basic]%
          {mud::io::basic\_socket}%
          {}%
          {
          };
       \umlclass[x=4,y=3,name=ip]%
          {mud::io::ip::socket}%
          {}%
          {
          };
       \umlclass[x=0,y=0,name=tcp]%
          {mud::io::tcp::socket}%
          {}%
          {
          };
       \umlclass[x=8,y=0,name=udp]%
          {mud::io::udp::socket}%
          {}%
          {
          };
       \draw[inheritance] (tcp.north) -- (ip.south west);
       \draw[inheritance] (udp.north) -- (ip.south east);
       \draw[inheritance] (ip.north) -- (basic.south);
    \end{tikzpicture};
  }
  \caption{Network layer socket class diagram.}
  \label{fig:mud-io-socket-class}
\end{figure}

Figure~\ref{fig:mud-io-socket-class} illustrates the relationship of all the
classes involved in the Network and Transport Layer sockets. The Network Layer
sockets all derive from the \indextt{basic\_socket} and \indextt{ip::socket}
base classes.  The Transport Layer sockets are defined by the
\indextt{tcp::socket} and \indextt{udp::socket} classes.  Only the latter ones
provide the functionality to actually send and receive data.

\subsection{TCP}

The TCP\index{TCP} sockets define the connection-oriented TCP protocol, upon
which numerous other protocols are derived from. This level of communication
provides a reliable communication channel between two peers. In order to
establish a connection, one side must be willing to accepting incoming
connection requests (often referred to as the server-side) and this side must
be actively listening for incoming connections before the other side (often
referred to as the client-side) to establish a connection. Once a connection
has been established between two peers, all communication between them is
channeled over a unique connection. The server-side may continue to listen
to additional incoming requests from other clients, and each successful
connection establishment will be given its own unique communication channel.

Each side of this communication channel is represented by an instance of the
\indextt{tcp::socket}. The associated kernel resource that unique defines the
peer's side of the communication channel is a \indextt{kernel\_handle} that is
owned by the \indextt{basic\_socket} base class. Only \textit{one} instance of
a socket owns this resource handle. Therefore, it is not possible to copy a
socket to another socket (but they can be referenced or they can be moved to
another socket instance).

The TCP protocol defines a unique connection by the combination of a host
address and a port number. This combination is represented by the
\indextt{tcp::endpoint} class. The endpoint is used by both the server-side to
define the connection paramters to listen to and for a client-side endpoint to
connect to. Once a connection has been established, a new port is allocated on
the server-side to represent the communication channel that has been
established. As a new port is being used, the original port can still be
re-used for accepting additional client-side connections. 

\begin{figure}[hbt]
  \centering
  \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
      \umlclass[x=0,y=8]%
          {mud::io::tcp::socket}%
          {}%
          {+ socket() \\
           + {\textit \~{}socket} \\
           + istr() \\
           + ostr() \\
          };
      \umlclass[x=-5,y=7.2]%
          {std::ostream}%
          {}%
          {
          };
      \umlclass[x=5,y=7.2]%
          {std::istream}%
          {}%
          {
          };
      \umlclass[x=-5,y=0,name=ostream-streambuf]%
          {mud::io::streambuf}%
          {}%
          {+ overflow(int): int \\
          };
      \umlclass[x=5,y=0,name=istream-streambuf]%
          {mud::io::streambuf}%
          {}%
          {+ underflow() : int \\
          };
       \umlclass[x=0,y=3,name=handle]%
          {mud::io::kernel\_handle}%
          {}%
          {
          };

       \draw[aggregation] (stdostream.east) -- (mudiotcpsocket.west);
       \draw[aggregation] (stdistream.west) -- (mudiotcpsocket.east);
       \draw[aggregation] (handle.north) -- (mudiotcpsocket.south);
       \draw[dependency]  (stdostream) -- (ostream-streambuf);
       \draw[dependency]  (stdistream) -- (istream-streambuf);
       \draw[dependency]  (ostream-streambuf.east) -| +(2,2.5) (handle);
       \draw[dependency]  (istream-streambuf.west) -| +(-2,2.5) (handle);
    \end{tikzpicture};
  }
  \caption{\texttt{mud::io::tcp} class diagram.}
  \label{fig:mud-io-tcp-class}
\end{figure}

In order to provide sending and receiving capabilities of communication data,
the \indextt{tcp::socket} contains their own input and output streams as
defined in figure~\ref{fig:mud-io-tcp-class}. Both stream buffers have access
to the socket handle resource in order to perform the sending and receiving
of the data. How the communication protocol is defined, in terms of the
format of the messages, whether or not a peer is expected to send or recieve
data, is out of the scope of this manual and is very much protocol specific.
The classes provided by \MUDLib are just the foundation stones on which the
protocol specific definitions can be implemented.

As alluded before, the TCP protocol defines one side to be actively listening
while the other side is trying to establish a connection. Both actions are
very specific to either side and are supported by two different helper classes.

\subsubsection{Server-Side}

On the server-side, an endpoint is to be allocated to start listening for, and
accepting incoming connection requests. This is handled by the
\indextt{tcp::acceptor} class. This controller class requires a listening
socket resource and an endpoint. Once a connection has been established, a new
socket denoting this new connection is returned from the
\texttt{tcp::acceptor}.

There are, in broad terms, two ways to wait for an incoming connection. Either
the accepting class is blocking\index{blocking I/O} the thread until such a
connection has been established, or it can wait for a signal until the
listening socket has detected an incoming connection request. The latter
approach (also referred to non-blocking\index{non-blocking I/O} I/O, is using
the \indextt{kernel\_event\_loop} to set-up the mechanism for waiting for a
connection, and is the default mechanism.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Non-blocking \texttt{tcp::acceptor}.},
  label=lst:non-blocking-tcp-acceptor]
// Create the acceptor
mud::io::tcp::acceptor acceptor();
acceptor.on_accept([](mud::io::tcp::socket&& client) {
    // Connection has been accepted and client socket
    // is available.
}
acceptor.open(mud::io::tcp::endpoint("127.0.0.1", 12345));
\end{lstlisting}

\subsubsection{Client-Side}

Similar to the server-side, the client-side has a controller class that is
responsible for perfoming the connection request. The \indextt{tcp::connector}.
A default socket is used as a placeholder for when the connection has been
established. The \texttt{tcp::connector} will attempt to connect to a specified
\indextt{tcp::endpoint}.

A non-blocking connection attempt requires more overhead, but allows more
control on the connection process. For example, it can be used to wait for a
connection on a different thread, or it timeout the connection attempt. An
example is provided in listing~\ref{lst:non-blocking-tcp-connector}.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Non-blocking \texttt{tcp::connector}.},
  label=lst:non-blocking-tcp-connector]
// Create the acceptor
mud::io::tcp::connector connector();
connector.on_connect([](mud::io::tcp::socket&& server) {
    // Connection has been accepted and server socket
    // is available.
}
connector.open(mud::io::tcp::endpoint("127.0.0.1", 12345));
\end{lstlisting}

\subsubsection{Communications}

Once a connection has been established, the communication over the socket
connection can be maintained by the \indextt{tcp::communicator} class. It takes
over the ownership of the socket and provides access to the socket's input and
output streams.  It also registers itself as a handler for any received message
at the \texttt{kernel\_event\_loop}. 

\subsection{UDP}

The UDP\index{UDP} socket uses a connection-less protocol to communicate
between two peers. There is no concept of actively accepting and connecting
peers, but messages are just plainly sent and received. As it is a
connection-less protocol, it cannot be directly verified if a message has
actually been received by a peer. Once a message has been received, it is
generally be replied to by using the source address in the UDP packet as
the destination address of the reply.

Each side of this communication channel is represented by an instance of the
\indextt{udp::socket}. The associated kernel resource that unique defines the
peer's side of the communication channel is a \indextt{kernel\_handle} that is
owned by the \indextt{basic\_socket} base class. Only \textit{one} instance of
a socket owns this resource handle. Therefore, it is not possible to copy a
socket to another socket (but they can be referenced or they can be moved to
another socket instance).

The UDP protocol defines a unique connection by the combination of a host
address and a port number. This combination is represented by the
\indextt{udp::endpoint} class. Thech packet contains a source and destination
endpoint and can be used to send messages and respond with replies. An initial
receiving side requires to bind to a specified address and port such that the
operating system knows to what application to route the UDP packets to. A
sending application can also bind to a address (but is not a requirement) in
order to define the address and port to bind to (i.e. to define a particular
network interface for communications).

\begin{figure}[hbt]
  \centering
  \resizebox{\textwidth}{!}{%
    \begin{tikzpicture}
      \umlclass[x=0,y=8]%
          {mud::io::udp::socket}%
          {}%
          {+ socket() \\
           + {\textit \~{}socket} \\
           + istr() \\
           + ostr() \\
          };
      \umlclass[x=-5,y=7.2]%
          {std::ostream}%
          {}%
          {
          };
      \umlclass[x=5,y=7.2]%
          {std::istream}%
          {}%
          {
          };
      \umlclass[x=-5,y=0,name=ostream-streambuf]%
          {mud::io::streambuf}%
          {}%
          {+ overflow(int): int \\
          };
      \umlclass[x=5,y=0,name=istream-streambuf]%
          {mud::io::streambuf}%
          {}%
          {+ underflow() : int \\
          };
       \umlclass[x=0,y=3,name=handle]%
          {mud::io::kernel\_handle}%
          {}%
          {
          };

       \draw[aggregation] (stdostream.east) -- (mudioudpsocket.west);
       \draw[aggregation] (stdistream.west) -- (mudioudpsocket.east);
       \draw[aggregation] (handle.north) -- (mudioudpsocket.south);
       \draw[dependency]  (stdostream) -- (ostream-streambuf);
       \draw[dependency]  (stdistream) -- (istream-streambuf);
       \draw[dependency]  (ostream-streambuf.east) -| +(2,2.5) (handle);
       \draw[dependency]  (istream-streambuf.west) -| +(-2,2.5) (handle);
    \end{tikzpicture};
  }
  \caption{\texttt{mud::io::udp} class diagram.}
  \label{fig:mud-io-udp-class}
\end{figure}

In order to provide sending and receiving capabilities of communication data,
the \indextt{udp::socket} contains their own input and output streams as
defined in figure~\ref{fig:mud-io-udp-class}. Both stream buffers have access
to the socket handle resource in order to perform the sending and receiving
of the data. How the communication protocol is defined, in terms of the
format of the messages, whether or not a peer is expected to send or recieve
data, is out of the scope of this manual and is very much protocol specific.
The classes provided by \MUDLib are just the foundation stones on which the
protocol specific definitions can be implemented.

There are, in broad terms, two ways to  for socket communication. Either
the socket class is blocking\index{blocking I/O} the thread until a message
has been received, or it can wait for a signal until the socket has detected
an incoming message. The latter approach (also referred to
non-blocking\index{non-blocking I/O} I/O, is using the
\indextt{kernel\_event\_loop} to set-up the mechanism for waiting for a
connection, and is the default mechanism.

\subsubsection{Server-Side}

On the server-side, an endpoint is to be allocated to start receiving
UDP messages. In contrast to a TCP connection there is no real distinction
or controller class to define the server or client side. This manual refers
to the server-side to the endpoint that sets up the initially receiving
part of the message.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Non-blocking \texttt{tcp::acceptor}.},
  label=lst:non-blocking-tcp-acceptor]
mud::io::udp::socket server();
server.bind(mud::io::tcp::endpoint("127.0.0.1", 12345));
acceptor.on_receive([](mud::io::tcp::socket&& client) {
    // Message is ready to be received from the client.
}
\end{lstlisting}

\subsubsection{Client-Side}

On the client-side, the side the initially sends the message, all that is
required is the endpoint that should receive the message. All that is
required is to send the message and have a response handler defined on any
reply message.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Non-blocking \texttt{tcp::connector}.},
  label=lst:non-blocking-tcp-connector]
mud::io::udp::socket client();
client.ostr(mud::io::tcp::endpoint("127.0.0.1", 12345)) 
acceptor.on_receive([](mud::io::tcp::socket&& server) {
    // Reply message is ready to be received from the server.
}
\end{lstlisting}

\subsubsection{Communications}

Once a socket has been created, the communication over the socket
connection can be maintained by the \indextt{udp::communicator} class. It
takes over the ownership of the socket and provides access to the socket's
input and output streams.  It also registers itself as a handler for any
received message at the \texttt{kernel\_event\_loop}. 

\subsection{Socket Options}
\index{socket options}

A plethora of options can be provided to socket to fine-tune its behaviour
in certain scenarios. For example, when accepting an incoming TCP connection
request, the listening socket is not always immediately available to accept
another incoming request. This can lead to a socket in a \texttt{TIME]\_WAIT}
state and a connection attempt will subsequently not be possible. A socket
option to re-use the endpoint address is available to eliminate this
scenario. Another option is to allow a \indextt{tcp::connector} to perform a
non-blocking connection attempt such that it can be passively monitored by
another thread until the connection has been made, or until a particular
connection timeout has expired.

These options can all be provided though the \texttt{option} template that is
available on the Network and Transport Layer socket definitions. Only those
options that are applicable to a certain type of socket can be applied
successfully to the \texttt{option} function. An incompatible option will
result in a compilation error.

For example, the \indextt{tcp::acceptor} will always set the
\indextt{ip::reuse\_address} socket option as specified in listing~
\ref{lst:reuse-address}.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Setting the reuse-address socket option.},
  label=lst:reuse-address]
// Set the reuse-address option
mud::io::tcp::socket listen_socket;
listen_socket.option<bool, mud::io::ip::reuse_address>(true);

// Retrieve hte value of the reuse-address option
bool value = listen.option<bool, mud::io::ip::reuse_address>();
\end{lstlisting}

The \texttt{option} method defines the option associated value type and the
option functor class. The argument to the \texttt{option} method it the value
to set for that option. In the example listing, the \indextt{ip::reuse\_address}
option expects a boolean value. Specifying the value \texttt{true} indicates
to enable the reuse of address endppints.

Similarly, there is also an \texttt{option} method to retrieve the value of
a particular option. An example is provided in listing~\ref{lst:reuse-address}.

% ============================================================================
% PART
% ============================================================================

\part{User Interface}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Basic Concepts}
\label{chp:ui-comcepts}

At the heart of the user-interface is the window\index{window}. This concept
is identical through all supported display mechanism and defines an
particular area which is assigned to be under control by the application.
This area is used for placing user interfaxe controls and drawing graphics.

Interaction with a user is performed by listening to input devices (for
example mouse movements and clicks, or keybaord presses) when these are
directed to the window. The application typically waits contineously for
any incoming user interface \it{events} by running a special event-loop. As
some display systems require that this user-inetrface event loop is run on
the main thread (the first thread) of the application, the \MUDLib
\indextt{application} loop must be run on the main thread as well. All
user-interface related handling will also be directed towards this main
thread internally. This means that when another thread wishes to draw on
the window canvas or change the user-interface controls, it has to post
these actions in a specialised \indextt{ui::task} which will be executed
by the application loop running on the main thread.

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Windows}
\label{chp:ui-windows}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Pushbuttons}
\label{chp:ui-pushbuttons}

% ============================================================================
% PART
% ============================================================================

\part{Test}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Gherkin}
\label{chp:test-gherkin}

Behaviour Driven Development~\index{BDD|see {Behaviour Driven Development}}%
\index{Behaviour Driven Development} is a process that encourages software
development to focus on the behaviour of a certain feature. This is
achieved by providing a clear and structured method of describing a feature,
its benefit, the stakeholders, actions and expected outcomes in a
non-technical way:

\begin{itemize}
  \item[] \textbf{Title}: An explicit title
  \item[] \textbf{Narrative}: Description of a feature
  \begin{itemize}
    \item[] \textbf{As a}: Person or role
    \item[] \textbf{I want}: A feature
    \item[] \textbf{So that}: Benefit or value
  \end{itemize}
  \item[] \textbf{Acceptance Criteria}
  \begin{itemize}
    \item[] \textbf{Given}: An initial context
    \item[] \textbf{When}: An action is performed
    \item[] \textbf{Then}: An observable and expected outcome
  \end{itemize}
\end{itemize}

Gherkin~\index{Gherkin} is an adopted language by the wider agile community
which is tightly associated to Behaviour Driven Development. Each feature
would have a set of scenarios, written roughly in the same narrative as
presented above, that define the acceptance criteria of that particular
feature. 

\MUDLib has adopted roughly the same language principles in its the testing
module and has provided the language elements as a set of \Cpp macros. 
These macros are defined. as follows:

\begin{description}
  \item[\indextt{CONTEXT()}] \hfill \\
        Defines the context that is used by the \texttt{FEATURE}. A constructor
        and destructor can be defined for any additional functionality to
        be executed before and after each \texttt{SCENARIO} test run. The
        name of the context class is \indextt{context}.
  \item[\indextt{END\_CONTEXT()}] \hfill \\
        Marks the end of a context.
  \item[\indextt{FEATURE(ID)}] \hfill \\
        Defines the feature by specifying the ID (as a string) and the
        context type that is used by all the scenarios and their steps.
  \item[\indextt{DEFINE\_GIVEN(ID,FUNC)}] \hfill \\
        Defines a pre-defined \textbf{Given} step with a feature unique ID and
        associated function that sets up the initial context.
  \item[\indextt{DEFINE\_WHEN}] \hfill \\
        Defines a pre-defined \textbf{When} step with a feature unique ID and
        associated function that performs a particular action.
  \item[\indextt{DEFINE\_THEN}] \hfill \\
        Defines a pre-defined \textbf{Then} step with a feature unique ID and
        associated function that performs a particular assertion on an
        observable outcome.
  \item[\indextt{END\_DEFINES}] \hfill \\
        Marks the end of all the predefined steps. This macro should
        \textit{always} be called between the feature and scenario definitions.
  \item[\indextt{SCENARIO(ID)}] \hfill \\
        Defines a new scenario.
  \item[\indextt{GIVEN(...)}] \hfill \\
        Defines \textbf{Given} step. If only an ID is specified, then the step
        refers to a pre-defined step. Otherwise it should be provided with a
        function definition. It is used to set-up the initial context of the
        scenario.
  \item[\indextt{WHEN(...)}] \hfill \\
        Defines \textbf{When} step. If only an ID is specified, then the step
        refers to a pre-defined step. Otherwise it should be provided with a
        function definition. It is used to specify the action to perform.
  \item[\indextt{THEN(...)}] \hfill \\
        Defines \textbf{Then} step. If only an ID is specified, then the step
        refers to a pre-defined step. Otherwise it should be provided with a
        function definition. It is used to verify the observable outcome by
        using \textbf{ASSERT} macros.
  \item[\indextt{AND(...)}] \hfill \\
        Defines an additional \textbf{GIVEN}, \textbf{WHEN} or \textbf{THEN}
        specification.
  \item[\indextt{DATA(...)}] \hfill \\
        Defines a data-table with an (optional) list of column names. If no
        column names are provided, the number of columns are determined by
        the \texttt{ROW} specification. \hfill \\
        The data table can be provided as part of any \texttt{GIVEN},
        \texttt{WHEN}, \texttt{THEN} and \texttt{AND} and will be only
        acessible within that particular step. The data is available through
        the \indextt{base\_context}.
  \item[\indextt{DATA\_ROW(...)}] \hfill \\
        Define a row in the data-table.
  \item[\indextt{END\_DATA()}] \hfill \\
        Marks the end of a data-table.
  \item[\indextt{SAMPLES}] \hfill \\
        Defines a sample-table with an (optional) list of column names. If no
        column names are provided, the number of columns are determined by
        the \texttt{ROW} specification. \hfill \\
        The sample-table can be provided as part of a \texttt{SCENARIO}. The
        scenario is run for every sample in the table, and only the contents
        of a particular sample is available though the \indextt{base\_context}.
  \item[\indextt{SAMPLE(...)}] \hfill \\
        Define a sample in the sample-table.
  \item[\indextt{END\_SAMPLES()}] \hfill \\
        Marks the end of a sample-table.
  \item[\indextt{END\_FEATURE()}] \hfill \\
        Marks the end of a feature.
  \item[\indextt{FEATURE\_RUN(...)}] \hfill \\
        Run all the registered features and their scenarios and return the
        overall result as a pair of \texttt{size\_t} values. The first value
        indicating the total number of scenarios being run, and the second
        value the total number of success. The total number of failed ones can
        be deduced by taking the difference of these two. \hfill \\
        An optional filter can be supplied to execute only a particular
        feature and/or scenario. This is of the form of
        \begin{description}
          \item \textit{feature} \hfill \\
                Run all the scenarios of the specified \textit{feature}
          \item \#\textit{scenario} \hfill \\
                Run all the specified \textit{scenario} in any feature.
          \item \textit{feature}\#\textit{scenario} \hfill \\
                Run a specific \textit{scenario} in a specific \textit{feature}.
        \end{description}
\end{description}

\noindent
The functions that are provided with the steps is often a lambda function with
the context-type as a reference parameter.

When verifying the expected outcome, a number of assertion macros can be used:

\begin{description}
  \item[\indextt{ASSERT(OUTCOME,EXPECTED)}] \hfill \\
        Verifies if the outcome is equal to the expected and raises an
        assertion exception if it doesn't.
  \item[\indextt{ASSERT\_THROW(EX,STATEMENTS)}] \hfill \\
        Verifies if the statements throw an exception of a particular type,
        and raises an assertion exception if it doesn't.
  \item[\indextt{ASSERT\_NO\_THROW(STATEMENTS)}] \hfill \\
        Verifies if the statements do not throw an exception, and raises an
        assertion exception if it doesn't.
\end{description}

As an example of the usage of the macros, consider listing~\ref{lst:gherkin}.
It defines a simple calculator, that has two variable input field and a
result value. These are defined in the \texttt{context}. This structure is
used as the context-type of the feature and is used with each scenario. Every
invocation of a scenario will use a new copy of the context. This ensures
that there can be no dependency between scenarios, where one scenario is
depending on the result of a previous scenario. Furthermore, the constructor
of the context type can be used to initialise any value prior to the run of
each scenario. And the destructor can be used likewise as the end of each
scenario run to clean-up afterwards.

It is advised that only one feature is defined within one \Cpp source file.
This eliminates compilation and linkage issues when more than one feature
is used. Event though the feature macro will create the same feature class
name, the macro wraps the feature in an unnamed namespace. This ensure that
the visibility is restricted to the internal compilation unit. The same
mechanism can be applied with the context class. If they are wrapped in an
unnamed namespace, other features in different compilation units can still use
the same context class name, but they are treated to be different types.

The calculator has a basic calculation feature that is capable of adding and
multiplying the two input fields. These two actions are separated and
verified by two acceptance criteria, one for addition and one for
multiplication. The example uses predefined \textbf{Given} and \textbf{When}
steps, but they could also be specfied by supplying their details as a
second parameter to the associated steps in the scenario. The verification
of the expected outcome is provided inline with the scenario and asserts the
result field of the context as the observable and testable outcome.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Gherkin test example},
  label=lst:gherkin]
// The calculator context
CONTEXT()
    int a, b, result;
    std::vector<int> input;
END_CONTEXT()

FEATURE("Basic Calculations")

  // Pre-defined steps
  DEFINE_GIVEN("first value of 2", [](context& ctx) {
        ctx.a = 2; })
  DEFINE_GIVEN("second value of 3", [](context& ctx) {
        ctx.b = 3; })

  DEFINE_WHEN("calculating the sum", [](context& ctx) { 
        ctx.result = ctx.a + ctx.b; })
  DEFINE_WHEN("calculating the product", [](context& ctx) { 
        ctx.result = ctx.a * ctx.b; })

  END_DEFINES()

  SCENARIO("Addition")
    GIVEN ("first value of 2")
      AND ("second value of 3")
    WHEN  ("calculating the sum")
    THEN  ("the sum is 5", [](context& ctx) {
            ASSERT(ctx.result, 5); })

  SCENARIO("Multiplication")
    GIVEN ("first value of 2")
      AND ("second value of 3")
    WHEN  ("calculating the product")
    THEN  ("the product is 6", [](context& ctx) {
            ASSERT(ctx.result, 6); })
\end{lstlisting}

It does become obvious that repeating addition and mulitplication scenarios
with different values can become quite cumbersome. To allow running a
particular multiple times, but with a different set of values every time it
is run, a set of \textit{samples} can be provided. For example, to run
an addition for a number of time, one can write a table as in
listing~\ref{lst:gherkin-samples}. The sample information is accessible through
the \texttt{context::sample} method. This allows the step definitions to access
the actual data of a \textit{(field)} entry by using the templated
\indextt{entry} function:

\begin{verbatim}
template<typename T>
T entry(const std::string& field);

template<typename T>
T entry(int field);
\end{verbatim}

\noindent
There is a version for both named fields and unnamed fields (the latter
is accessible using a zero-based index number). The templated function returns
the field information converted to the desired type \texttt{T}.
 
\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Gherkin test example},
  label=lst:gherkin-samples]
SCENARIO("Addition")
  GIVEN ("first value of <first>", [](context& ctx) {
         ctx.a = ctx.sample().entry<int>("first"); })
    AND ("second value of <second>", [](context& ctx) {
         ctx.b = ctx.sample().entry<int>("second"); })
  WHEN  ("calculating the sum")
  THEN  ("the sum is <result>", [](context& ctx) {
          ASSERT(ctx.result, ctx.sample().entry<int>("result")); })
  SAMPLES("first", "second", "result")
      SAMPLE(0, 0, 0)
      SAMPLE(4, 0, 4)
      SAMPLE(0, 4, 4)
      SAMPLE(5, 4, 9)
  END_SAMPLES()
\end{lstlisting}

In a similar fashion, tables of any data can be provided at each \texttt{GIVEN},
\texttt{WHEN}, \texttt{THEN} and \texttt{AND} step. The data is accessible
inside these particular steps only. For example, listing~\ref{lst:gherking-data}
presents an example where a table is used to simulate that a user inputs
a number of values. The data information is accessible through the
\indextt{context::data} method. The data information at a \textit{(row, column)}
can be retrieved through a templated \indextt{entry} function:

\begin{verbatim}
template<typename T>
T entry(int row, const std::string& column);

template<typename T>
T entry(int row, int column);
\end{verbatim}

\noindent
There is a version for both named columns and unnamed columns (the latter
is accessible using a zero-based index number). The templated function returns
the field information converted to the desired type \texttt{T}.

\begin{lstlisting}[%
  frame=single,
  captionpos=b,
  caption={Gherkin test example},
  label=lst:gherkin-data]
SCENARIO("Average")
  GIVEN ("a list of values", [](context& ctx) {
         for (int i = 0; i < ctx.data().row_count(); ++i) {
             ctx.input.push_back(ctx.data().entry<int>(i, "value"));
         }})
    DATA("value")
        DATA_ROW(5)
        DATA_ROW(6)
        DATA_ROW(7)
    END_DATA()
  WHEN  ("calculating the average", [](context& ctx) {
             int total = 0;
             for(auto value : ctx.input) {
               total += value;
             }
             ctx.result = total / ctx.input.size();
         })
  THEN  ("the average is 6", [](context& ctx) {
          ASSERT(ctx.result, 6); })
\end{lstlisting}

% ============================================================================
% CHAPTER
% ============================================================================

\chapter{Implementation}
\label{chp:test-implementation}

The usage of the macros is enough in most cases, and is also encouraged to
do so. However, it may be required to extend the implementation

\section{Exception Classes}
\label{sct:test-exception-classes}

\subsection{\texttt{assertion\_failed}}

The \indextt{assertion\_failed} exception is used to indicate that an test-case
has failed a testable condition. These are captured by the feature test
runner and their details are recorded. These exceptions would therefore not
fail the total execution of the test run, but are used to record that a particular test scenario has failed.

\subsection{\texttt{not\_specified}}

When setting up test scenarios within a feature, it is a common practice to
define elements that are re-used by multiple scenarios only once and let each
scenario reference these generically defined elements. However, when a
reference is not known, it shall raise a \indextt{not\_specified} exception.
These exceptions are captured by the feature test running and their details
are recorded. These exceptions would therefore not fail the total execution of
the test run, but are used to record that a particular test scenario has
failed.

\section{Gherkin classes}

A number of classes are defined, one for each Gherkin item, and include the
\indextt{\_given}, \indextt{\_when} and \indextt{\_then} classes. Each of them
hold a single Gherkin step and have the ability to chain other similar items by
means of the \texttt{And} method (note that \texttt{and} is a keyword in \Cpp,
the method name differs from the coding standard by having a capital first
letter).  The \indextt{\_scenario} and \indextt{\_feature} types refer to the
Gherkin steps and predefined Gherkin steps. 

Note that the naming convention for the Gherkin implementation class names is
to prepend their name with an underscore. This is a signal that the class is
\textit{hidden}, but it does not stop the user from using them.

When creating a new feature (as accomplished through the \Cpp macros), that
feature is defined in a new class that is derived from \texttt{\_feature},
with its constructor populating the predefined Gherkin steps. As the
\texttt{\_feature} is an abstract class (as the \indextt{\_base\_feature} has
an abstract \texttt{run} method), the new feature class will need to implement
the \texttt{run} method. This method should create all the scenarios and run
them, while accumulating their results. 

\section{Feature Factory}

All the defined feature classes are auto-registered to the
\indextt{\_feature\_factory}. A convenience template wrapper is available,
\indextt{\_feature\_registrar}, that can be used as a file-scope global
variable, to facilitate in the registration.

A macro can be used to execute all registered features, or it can be
manually executed through its static \texttt{run} method.

% ============================================================================
% PART
% ============================================================================

\part{Reference}

% ============================================================================
% Bibliography
% ============================================================================

\newpage
\printbibliography

% ============================================================================
% Index
% ============================================================================

\newpage

\indexprologue{An index entry that is type-set in \texttt{tele-type} represents
a name of a \Cpp class or function.}

\printindex

\end{document}
