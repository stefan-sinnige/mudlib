# Define the subfolders and the order in which they are to be built.
SUBDIRS = test core xml html event io protocols samples doc

# Package config
pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = mudlib.pc

# vi: set ts=4 noexpandtab:

